<header class="page-spacer">
    <a href="{{ path('common_main_index') }}" class="logo">
        <img src="{{ asset('build/images/logo-tastehub.svg') }}" alt="Logo où est inscrit TasteHub agrémenté d'une toque">
    </a>

    <nav>
        <ul>
            <li class="appbar-only">
                <a href="{{ path('common_main_index') }}">
                    <i class="icon icon-home"></i>
                    <span>Accueil</span>
                </a>
            </li>
            <li>
                <a href="{{ path('cooking_recipe_explore_my_book') }}">
                    <i class="icon icon-chef-hat"></i>
                    <span>Livre</span>
                </a>
            </li>
            <li>
                <a href="{{ path('cooking_recipe_explore_index') }}">
                    <i class="icon icon-compass"></i>
                    <span>Explorer</span>
                </a>
            </li>
            <li>
                {% if app.user %}
                    {# TODO - Add link to manage account #}
                    <a href="#">
                        <i class="icon icon-user"></i>
                        <span class="appbar-only">Compte</span>
                        <span class="nav-only">{{ app.user.username }}</span>
                    </a>
                {% else %}
                    <a href="{{ path('user_security_login') }}">
                        <i class="icon icon-user"></i>
                        <span class="appbar-only">Compte</span>
                        <span class="nav-only">Me connecter</span>
                    </a>
                {% endif %}
            </li>
        </ul>
    </nav>
</header>
