{% extends 'pages/cooking/recipe-form/layout.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('cooking_recipe_form_ingredients') }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('cooking_recipe_form_ingredients') }}
{% endblock %}

{% block form %}
    <div data-recipe-item-collection>
        <div class="row-group tiny item-source">
            {{ form_row(form.recipeIngredientGenerator.ingredient) }}
            {{ form_row(form.recipeIngredientGenerator.quantity) }}
            {{ form_row(form.recipeIngredientGenerator.unit) }}
        </div>

        <button type="button" class="button item-add-trigger">
            Ajouter l'ingrédient
        </button>

        {{ form_row(form.recipeIngredients) }}
    </div>
{% endblock %}
